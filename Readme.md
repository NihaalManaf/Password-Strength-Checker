This is a simple password strength checker that works by calculating the negative log loss on a password based on a bigram modal trained on a popular password wordlist, `rockyou.txt` used commonly in penetration testing.

